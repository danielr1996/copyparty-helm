apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "copyparty-helm.fullname" . }}-test-connection"
  labels:
    {{- include "copyparty-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "copyparty-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
